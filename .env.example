# ─── Supabase ────────────────────────────────────────────────────────────────
# Get from: supabase.com → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ─── Stripe (TEST mode) ───────────────────────────────────────────────────────
# Get from: dashboard.stripe.com → Developers → API keys → toggle to "Test mode"
# Keys look like: pk_test_51... and sk_test_51...
# NEVER commit live keys (pk_live_ / sk_live_). Rotate immediately if exposed.
STRIPE_SECRET_KEY=sk_test_YOUR_TEST_SECRET_KEY_HERE
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SIGNING_SECRET_HERE
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_TEST_PUBLISHABLE_KEY_HERE

# Weekly price ID — create in Stripe Dashboard:
# Products → Add product → $2.49 → Recurring → Weekly
NEXT_PUBLIC_STRIPE_PRICE_ID=price_YOUR_WEEKLY_PRICE_ID_HERE

# ─── Dev / Testing ────────────────────────────────────────────────────────────
# Set to 'true' to enable mock subscription overrides (localStorage-based).
# Lets you test paywall without real Stripe payment. NEVER set true in production.
NEXT_PUBLIC_DEV_MOCK_ENABLED=true
